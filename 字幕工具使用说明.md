# AI字幕生成工具使用说明

## 核心文件

### 主要脚本
- **add_subtitle_whisper.ps1** - 一键自动化字幕生成脚本（推荐使用）
- **whisper_to_srt.py** - Whisper AI 语音识别生成字幕
- **correct_subtitle.py** - 使用原文本矫正字幕内容
- **add_subtitle_to_video.py** - 将字幕嵌入视频

## 快速开始

### 1. 准备工作
确保以下文件已准备：
- 视频文件（MP4格式）
- 原文本文件（UTF-8编码的txt文件）

### 2. 编辑配置
打开 `add_subtitle_whisper.ps1`，修改文件路径：
```powershell
$VIDEO_FILE = ".\output_video\你的视频.mp4"
$TEXT_FILE = "D:\video_workspace\source_txt\你的文本.txt"
```

### 3. 运行脚本
在PowerShell中执行：
```powershell
.\add_subtitle_whisper.ps1
```

## 工作流程

```
视频文件 → Whisper AI识别 → 原文本矫正 → 字幕嵌入视频 → 输出最终视频
```

### 步骤详解

1. **Whisper识别**：使用AI识别视频中的语音，生成带时间戳的字幕
   - 模型：base（速度快，准确度中等）
   - 语言：中文
   - 每行最多：13个字符

2. **文本矫正**：使用原始文本文件矫正Whisper识别的内容
   - 保留Whisper的精确时间戳
   - 替换为准确的原文本
   - 智能断句，避免出框

3. **字幕嵌入**：将矫正后的字幕嵌入视频
   - 字体：黑体（SimHei）
   - 字号：14
   - 颜色：黄色
   - 边框：黑色3像素
   - 阴影：2像素
   - 位置：底部居中

## 输出文件

- `output_video/whisper_字幕.srt` - Whisper原始识别字幕
- `output_video/corrected_字幕.srt` - 矫正后的字幕
- `output_video/最终视频.mp4` - 带字幕的最终视频

## 参数调整

### 修改字体大小
```powershell
--fontsize 14  # 默认14，可改为12-20
```

### 修改字体颜色
```powershell
--fontcolor yellow  # 可选: white, yellow, red, cyan
```

### 修改每行字符数
```powershell
--max-chars 13  # 默认13，建议10-15之间
```

## 常见问题

### Q: 字幕超出屏幕怎么办？
A: 减小 `--max-chars` 参数（如改为12或11）

### Q: 字幕看不清怎么办？
A: 增大 `--fontsize` 或更换对比度更高的颜色

### Q: Whisper识别不准怎么办？
A: 使用更大的模型（将 `--model base` 改为 `--model medium` 或 `--model large`）

### Q: 字幕和语音不同步怎么办？
A: 这个问题不应该出现，Whisper会自动生成精确时间戳。如果出现请检查视频文件格式。

## 技术细节

### Whisper模型
- `tiny` - 最快，准确度较低
- `base` - 平衡（默认推荐）
- `small` - 较准确，速度中等
- `medium` - 很准确，速度较慢
- `large` - 最准确，速度最慢

### 字幕格式
生成的是标准SRT格式：
```
1
00:00:00,000 --> 00:00:02,500
第一行字幕
第二行字幕

2
00:00:02,500 --> 00:00:05,000
下一条字幕
```

## 依赖要求

- Python 3.8+
- openai-whisper
- ffmpeg
- torch
- PowerShell 7+

## 版本历史

- v1.0 (2026-02-03): 初始版本，支持AI识别+文本矫正+自动嵌入
